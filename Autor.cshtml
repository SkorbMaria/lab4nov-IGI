@model IEnumerable<IGI_5.Models.Autor>
@{
    ViewData["Title"] = "Autors";
}

<br />
<br />
<br />
<br />
<h2>Autors</h2>

<p>
    <a asp-controller="User" asp-action="AddAutor">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                <a asp-action="Autor" asp-route-page="@ViewBag.PageViewModel.PageNumber" asp-route-sort="ID"> @Html.DisplayNameFor(model => model.ID)</a>
            </th>
            <th>
                <a asp-action="Autor" asp-route-page="@ViewBag.PageViewModel.PageNumber" asp-route-sort="About"> @Html.DisplayNameFor(model => model.About)</a>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.About)
                </td>
                <td>
                    <a asp-controller="User" asp-action="EditAutor" asp-route-id="@item.ID">Edit</a> |
                    <a asp-controller="User" asp-action="RemoveAutor" asp-route-id="@item.ID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
@if (ViewBag.PageViewModel.HasPreviousPage)
{
    <a asp-action="Autor" asp-route-page="@(ViewBag.PageViewModel.PageNumber - 1)" class="btn btn-default btn">
        <i class="glyphicon glyphicon-chevron-left"></i>
        Назад
    </a>
}
@if (ViewBag.PageViewModel.HasNextPage)
{
    <a asp-action="Autor" asp-route-page="@(ViewBag.PageViewModel.PageNumber + 1)" class="btn btn-default btn">
        Вперед
        <i class="glyphicon glyphicon-chevron-right"></i>
    </a>
}